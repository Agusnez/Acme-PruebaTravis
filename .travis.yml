sudo: required
language: java
jdk: openjdk7

services: mysql

before_script:
- sudo mysql -e "USE mysql; UPDATE mysql.user SET user='acme-user', password=PASSWORD('ACME-Us3r-P@ssw0rd') WHERE user='root'; FLUSH PRIVILEGES;"
- cd src/main/java/utilities
- ls
- javac PopulateDatabase.java
- java PopulateDatabase

script:
- mvn install -DskipTests=true -Dmaven.javadoc.skip=true -B -V

addons:
  apt:
   sources:
    - mysql-5.7-trusty
   packages:
    - mysql-server
    - mysql-client
